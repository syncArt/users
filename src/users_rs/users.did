type SocialMedia = record { nickname : text; name : text };
type User = record {
  name : text;
  description : text;
  social_media : vec SocialMedia;
};
service : {
  get : (text) -> (User) query;
  getSelf : () -> (User) query;
  search : (text) -> (opt User) query;
  update : (User) -> ();
}
