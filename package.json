{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "type": "module",
  "name": "smileyBall-users",
  "scripts": {
    "build": "exit 0",
    "prebuild": "exit 0",
    "pretest": "exit 0",
    "start": "exit 0",
    "buildBackend": "cargo build --release --target wasm32-unknown-unknown"
  }
}
